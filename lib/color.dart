import 'package:flutter/material.dart';
class ColorsChange extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold( appBar: AppBar(
              title: Text('Saved WordPairs',style: TextStyle(color:Colors.yellow),)));
      
  
  }
}